<center>

<h3>All Games</h3>

<table>
    <tr>
        <th>Location</th>
        <th>But what time?</th>
    </tr>
    <% @games.each do |game| %>
    <tr>
        <td><%= link_to game.court.name, court_path(game.court) %></td>
        <td><%= game.time.to_formatted_s(:gametime) %></td>
    </tr>
    <% end %>
</table>

</center>