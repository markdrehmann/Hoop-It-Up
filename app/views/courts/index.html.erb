<center>
<h3>Courts</h3>

<table>
    <tr>
        <th>Name</th>
        <th>Address</th>
    </tr>
    <% @courts.each do |court| %>
    <tr>
        <td><%= link_to court.name, court_path(court) %></td>
        <td><%= court.address %></td>
    </tr>
    <% end %>
</table>


</center>